{
    "title": "Toggle caps lock",
    "rules": [
        {
            "description": "Press both shift keys to toggle caps lock",
            "manipulators": [
                {
                    "type": "basic",
                    "from": <%= from("left_shift", ["right_shift"], ["any"]) %>,
                    "to": <%= to([["caps_lock"]]) %>,
                    "to_if_alone": <%= to([["left_shift"]]) %>
                },
                {
                    "type": "basic",
                    "from": <%= from("right_shift", ["left_shift"], ["any"]) %>,
                    "to": <%= to([["caps_lock"]]) %>,
                    "to_if_alone": <%= to([["right_shift"]]) %>
                }
            ]
        }
    ]
}
