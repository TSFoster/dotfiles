#!/bin/sh

touch ~/.hushlogin
mkdir -p $HOME/.config
ln -i ./powerline $HOME/.config/
ln -i ./git/home.gitconfig $HOME/.gitconfig
ln -i ./shell/home.inputrc $HOME/.inputrc
ln -i ./tmux/home.tmux.conf $HOME/.tmux.conf
ln -i ./irb/home.irbrc $HOME/.irbrc
